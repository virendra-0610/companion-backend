name: Weather Alert Scheduler

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 2

    steps:
      - name: Scheduler disabled
        run: |
          echo "GitHub Actions scheduler is disabled."
          echo "Use Vercel + cron-job.org backend instead."
